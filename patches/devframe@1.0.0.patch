diff --git a/dist/client/index.d.mts b/dist/client/index.d.mts
index 8db367b73d7aabfa28a93cb8641ab3010f1b8650..989c8027a9fec572607c2ae6f6e65f0dbe87a6e6 100644
--- a/dist/client/index.d.mts
+++ b/dist/client/index.d.mts
@@ -18,6 +18,15 @@ export interface DevframeConnection {
   authToken?: string;
 }
 export interface SetupDevframeConnectionOptions {
+  /**
+   * Keep connection discovery and credentials local to this RPC client.
+   * Skips shared window/localStorage reads and writes, and authentication
+   * broadcasts. Explicit connections, metadata and credentials still apply.
+   * Pass this option again when reusing a prepared connection.
+   *
+   * @default false
+   */
+  isolateConnection?: boolean;
   /** Reuse a complete connection prepared in another viewer or JavaScript realm. */
   connection?: DevframeConnection;
   /** Use a pre-known descriptor while deriving its source URL from `baseURL`. */
@@ -165,7 +174,7 @@ export interface DevframeScopedClientRpc<NS extends string = string> {
   };
   /** Resolve a namespaced shared state. Bare keys resolve within this namespace. */
   sharedState: {
-    <T extends keyof ScopedSharedStates<NS> & string>(key: T, options?: RpcSharedStateGetOptions<ScopedSharedStates<NS>[T]>): Promise<SharedState<ScopedSharedStates<NS>[T]>>;
+    <T extends Extract<keyof ScopedSharedStates<NS>, string>>(key: T, options?: RpcSharedStateGetOptions<ScopedSharedStates<NS>[T]>): Promise<SharedState<ScopedSharedStates<NS>[T]>>;
     <T extends Record<string, any> = Record<string, any>>(key: string, options?: RpcSharedStateGetOptions<T>): Promise<SharedState<T>>;
   };
   /** Streaming host scoped to this namespace. */
diff --git a/dist/client/index.mjs b/dist/client/index.mjs
index a26af04daee8a1d93897225fa8efa43b9b1efb32..28ef65345c8bd87fc30208d4912d07ff8aad7e5d 100644
--- a/dist/client/index.mjs
+++ b/dist/client/index.mjs
@@ -481,31 +481,27 @@ function getDevframeConnection() {
 * fetching `__connection.json` from the configured base URLs.
 */
 async function setupDevframeConnection(options = {}) {
-	if (options.connection) {
-		const connection = withAuthToken(options.connection, readStoredAuthToken(options.authToken ?? options.connection.authToken ?? options.connection.connectionMeta.authToken));
-		storeConnection(connection);
-		return connection;
-	}
+	const connection = await resolveDevframeConnection(options);
+	const authToken = options.authToken ?? connection.authToken ?? connection.connectionMeta.authToken;
+	const resolvedConnection = withAuthToken(connection, options.isolateConnection ? authToken : readStoredAuthToken(authToken));
+	if (options.isolateConnection) return resolvedConnection;
+	storeConnection(resolvedConnection);
+	return resolvedConnection;
+}
+async function resolveDevframeConnection(options) {
+	if (options.connection) return options.connection;
 	const bases = Array.isArray(options.baseURL) ? options.baseURL : [options.baseURL ?? "./"];
-	if (options.connectionMeta) {
-		const connection = {
-			connectionMeta: options.connectionMeta,
-			/**
-			* Preserve the established connectionMeta behavior: an explicitly
-			* supplied descriptor resolves from the caller's explicit base.
-			*/
-			metaBaseUrl: resolveMetaBaseUrl(bases[0] ?? "./"),
-			authToken: readStoredAuthToken(options.authToken ?? options.connectionMeta.authToken)
-		};
-		storeConnection(connection);
-		return connection;
-	}
-	const existing = getDevframeConnection();
-	if (existing) {
-		const connection = withAuthToken(existing, readStoredAuthToken(options.authToken ?? existing.authToken ?? existing.connectionMeta.authToken));
-		storeConnection(connection);
-		return connection;
-	}
+	if (options.connectionMeta) return {
+		connectionMeta: options.connectionMeta,
+		/**
+		* Preserve the established connectionMeta behavior: an explicitly
+		* supplied descriptor resolves from the caller's explicit base.
+		*/
+		metaBaseUrl: resolveMetaBaseUrl(bases[0] ?? "./"),
+		authToken: options.connectionMeta.authToken
+	};
+	const existing = options.isolateConnection ? void 0 : getDevframeConnection();
+	if (existing) return existing;
 	const errors = [];
 	for (const base of bases) {
 		const metaPath = withBase(DEVFRAME_CONNECTION_META_FILENAME, base);
@@ -515,7 +511,7 @@ async function setupDevframeConnection(options = {}) {
 			if (!response.ok) throw new Error(`Failed to fetch connection meta from ${metaUrl}: ${response.status}`);
 			const connectionMeta = await response.json();
 			const loadedFrom = response.url || metaUrl;
-			const connection = {
+			return {
 				connectionMeta,
 				/**
 				* A served `baseUrl` re-points relative resolution (RPC dump shards,
@@ -523,10 +519,8 @@ async function setupDevframeConnection(options = {}) {
 				* per-frame meta directs each frame SPA at the hub's own dump.
 				*/
 				metaBaseUrl: connectionMeta.baseUrl ? new URL(connectionMeta.baseUrl, loadedFrom).href : loadedFrom,
-				authToken: readStoredAuthToken(options.authToken ?? connectionMeta.authToken)
+				authToken: connectionMeta.authToken
 			};
-			storeConnection(connection);
-			return connection;
 		} catch (error) {
 			errors.push(error);
 		}
@@ -1804,6 +1798,13 @@ function resolveClientTransport(requested, meta) {
 	if (hasSse) return "sse";
 	throw new Error("[devframe] This server advertises no RPC transport (backend \"none\"), so there is nothing to connect to. Enable the WebSocket or SSE endpoint on the server, or use its static/MCP surfaces instead.");
 }
+function createAuthChannel(isolateConnection = false) {
+	if (isolateConnection) return void 0;
+	try {
+		/** Channel name kept for cross-tab interop with the Vite DevTools auth page. */
+		return new BroadcastChannel("devframe-auth");
+	} catch {}
+}
 async function getDevframeRpcClient(options = {}) {
 	const { baseURL = "./", rpcOptions = {}, cacheOptions = false } = options;
 	const events = createEventEmitter();
@@ -1863,10 +1864,14 @@ async function getDevframeRpcClient(options = {}) {
 		...liveModeOptions,
 		wsOptions: options.wsOptions
 	});
-	let authChannel;
-	try {
-		authChannel = new BroadcastChannel("devframe-auth");
-	} catch {}
+	const authChannel = createAuthChannel(options.isolateConnection);
+	function updateAuthToken(token) {
+		connection = {
+			...connection,
+			authToken: token
+		};
+		if (!options.isolateConnection) storeAuthToken(token);
+	}
 	let bootstrapAuthPromise;
 	let bootstrapAuthSettled = false;
 	function gateOnBootstrapAuth(fn) {
@@ -1910,21 +1915,13 @@ async function getDevframeRpcClient(options = {}) {
 		ensureTrusted: mode.ensureTrusted,
 		requestTrust: mode.requestTrust,
 		requestTrustWithToken: async (token) => {
-			storeAuthToken(token);
-			connection = {
-				...connection,
-				authToken: token
-			};
+			updateAuthToken(token);
 			return mode.requestTrustWithToken(token);
 		},
 		requestTrustWithCode: async (code) => {
 			const token = await mode.requestTrustWithCode(code);
 			if (!token) return false;
-			storeAuthToken(token);
-			connection = {
-				...connection,
-				authToken: token
-			};
+			updateAuthToken(token);
 			try {
 				authChannel?.postMessage({
 					type: "auth-update",
diff --git a/dist/context--tVkJw3W.d.mts b/dist/context--tVkJw3W.d.mts
index db223a17802de0b41ba214693546fa06aa4e0276..a5bd8bb625ab472fcfe74bc7f973043dbf83206c 100644
--- a/dist/context--tVkJw3W.d.mts
+++ b/dist/context--tVkJw3W.d.mts
@@ -893,7 +893,7 @@ interface DevframeScopedNodeRpc<NS extends string = string> {
    * namespace; pass a fully-qualified key (containing `:`) to opt out.
    */
   sharedState: {
-    <T extends keyof ScopedSharedStates<NS> & string>(key: T, options?: RpcSharedStateGetOptions<ScopedSharedStates<NS>[T]>): Promise<SharedState<ScopedSharedStates<NS>[T]>>;
+    <T extends Extract<keyof ScopedSharedStates<NS>, string>>(key: T, options?: RpcSharedStateGetOptions<ScopedSharedStates<NS>[T]>): Promise<SharedState<ScopedSharedStates<NS>[T]>>;
     <T extends Record<string, any> = Record<string, any>>(key: string, options?: RpcSharedStateGetOptions<T>): Promise<SharedState<T>>;
   };
   /** Streaming host scoped to this namespace. */
diff --git a/dist/ws-server-y2R76aSW.d.mts b/dist/ws-server-y2R76aSW.d.mts
index fdf74c88dedd9524ed5018350530193644f89717..81053a5ca782be1810cf43d8196412c4c2573dbc 100644
--- a/dist/ws-server-y2R76aSW.d.mts
+++ b/dist/ws-server-y2R76aSW.d.mts
@@ -1,7 +1,7 @@
 import { a as ChannelOptions, n as BirpcGroup } from "./index-Bia1vKjL.mjs";
 import { g as RpcFunctionDefinitionAny } from "./types-BmDbfHCx.mjs";
 import { n as DevframeRpcConnection, t as DevframeNodeRpcSessionMeta } from "./session-r6PDixP6.mjs";
-import { Hooks } from "crossws";
+import { Hooks } from "crossws/types";
 import { NodeAdapter } from "crossws/adapters/node";
 import { Buffer } from "node:buffer";
 import { IncomingMessage, Server } from "node:http";
