name: Install
description: Install the pinned monorepo toolchain
runs:
  using: composite
  steps:
    - uses: pnpm/action-setup@v5
    - uses: actions/setup-node@v6
      with:
        node-version: 26.9.0
        cache: pnpm
    - shell: bash
      run: pnpm install --frozen-lockfile
